/**
 * Re-export of the shared GatewayBrowserClient from the Lit UI.
 * This is imported directly from the ui/ package source to avoid duplication.
 */
export {
  GatewayBrowserClient,
  type GatewayBrowserClientOptions,
  type GatewayEventFrame,
  type GatewayResponseFrame,
  type GatewayHelloOk,
} from '@ui-shared/gateway'
